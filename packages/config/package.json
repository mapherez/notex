{
  "name": "@notex/config",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src/",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@notex/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}